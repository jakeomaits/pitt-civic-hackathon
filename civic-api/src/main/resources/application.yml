info:
  component: civic-api

logging:
  level:
    root: info

spring:
  jackson:
    default-property-inclusion: NON_NULL
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:adyen-tender-api
    driverClassName: org.h2.Driver
    username: sa
  h2:
    console:
      enabled: true
